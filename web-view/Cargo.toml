[package]
edition = "2021"
name = "web-view"
version = "0.1.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
clumsy-rt = {path = "../clumsy-rt"}
paddle = {path = "../../gamedev/paddlers/paddle"}# TODO: release another beta or something

wasm-bindgen = "0.2"
web-sys = "0.3"